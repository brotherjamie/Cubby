<html lang="en">
<%- include("../partials/head.ejs") %>
<body>
<%- include("../partials/nav.ejs") %>
<main>
<%- include("../partials/header.ejs") %>
<div class="background">
<%- include("../partials/footer.ejs") %>
<div class="pagebox">
<a name="top"></a>
<div class="pageheading">
 <h2 class="heading">The Cubby's Blog Page</h2>
</div>

<div class="page-pad">
 <% if (blogs.length > 0) { %>
  <% blogs.forEach(blog => { %>
   <a href="#/blog/<%= blog.id %>" class="bloghover">
     <div class="blogbox">
       <div class="inputrow">
         <div class="inputcolumn">
     <h4 class="bloghead"><%= blog.title %></h4>
     <p class="date"><%= blog.createdAt.toLocaleDateString() %></p>
     <p class="summary"><%= blog.summary %></p>
        </div>
        <div class="inputcolumn"><img height="250" class="cubbyimg" src="<%= blog.coverImagePath %>"></div>
         </div>
    </a>

 <div class="reveal blogbody" id="/blog/<%= blog.id %>">
  <a href="#"></a>
  <div><p><%= blog.body %></p></div>
</div> 
</div> 

<% }) %>
<% } else { %>
 <p>There are no blogs to display...</p>
<% } %>

<div class="central-button"><a href="#top"><button class="returntoarticles">back to top</button></a>
</div>

<div class="consclusion"><p>Have you discovered a new paint brand you want to share? Learnt a modelling technique that's too game changing to keep to yourself? Or do you simply want to show off your handywork on your latest model? We are happy to hear what you have to say about Scale Modelling! Use the contact button to send us a message and we can discuss adding your entry to the webpage. The more varied perspectives the better!<br><br></p></div>

</div>
</div>
<%- include("../partials/footer.ejs") %> 
</div>
</main>

<script>
$(document).ready(function(){
  $('a[href^="#"]').on('click',function (e) {
      e.preventDefault();

      var target = this.hash,
      $target = $(target);

      $('html, body').stop().animate({
          'scrollTop': $target.offset().top
      }, 900, 'swing', function () {
          window.location.hash = target;
      });
  });
});
</script>

</body>
</html>